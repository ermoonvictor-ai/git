# Real secrets and rendered configs — never commit these.
.env

# Rendered configs + all persistent state (database, signing keys, media,
# uploaded avatars, TLS certificates). Generated by scripts/setup.sh and
# by the containers themselves; never sourced from git.
synapse/data/
element/data/
caddy/data/
coturn/data/
